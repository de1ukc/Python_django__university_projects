import json
import math

c = 42
# Мой класс
class mycls:
    name = "Valery Albertych"
    age = 54

    def pupa_lupa(x = 1, y = 2):
        s = json.dumps("ABACABA")
        return math.sin(x + y + c), s